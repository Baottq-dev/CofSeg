# Sáu lượt của benchmark: mỗi ruộng làm test đúng một lần.
#
# Vì sao chia theo RUỘNG chứ không theo ảnh hay đường bay: hai ruộng là hai
# mảnh đất khác nhau, không có cặp ảnh nào chồng lấn bắc cầu giữa chúng, nên
# đây là cách chia duy nhất mà số trên test thật sự là "ruộng chưa thấy".
# Chia theo đường bay trong cùng ruộng vẫn không rò rỉ pixel, nhưng model đã
# thấy đúng giống cây, đúng đất, đúng độ cao bay — số sẽ đẹp hơn thực tế.
#
# Gộp 6 fold lại thì mọi ảnh trong bộ đều có dự đoán từ một model chưa thấy
# ruộng của nó -> bảng metric theo từng ruộng cho toàn bộ dữ liệu.
#
# File này KHÔNG khai val. Cách chia val khai ở file riêng, vì có ba cách và
# chúng cho ba bộ fold khác nhau — để cạnh nhau mà so, đừng chồng lên nhau:
#
#     configs/dataset/val_block.yaml    khối ảnh cuối mỗi đường bay, trượt theo lượt
#     configs/dataset/val_flight.yaml   trọn hai đường bay cuối, cố định
#     configs/dataset/val_field.yaml    trọn một ruộng, xoay vòng theo lượt
#
# Hai cách đầu cắt val ở mức ẢNH bên trong 5 ruộng train, nên vẫn train đủ 5
# ruộng như yêu cầu của đồ án. Cách thứ ba cố ý trả lại một ruộng để đổi lấy
# val nằm đúng điều kiện của test — train còn 4 ruộng, nhưng số ẢNH train gần
# như không đổi vì nó không tốn khoảng đệm nào.
#
# Dùng:
#     python scripts/make_fold.py --export data/export/dataset_v1 \
#         --val configs/dataset/val_block.yaml --all --out-root data/export/block

fields: [field_1, field_2, field_3, field_4, field_5, field_6]

folds:
  f1: {test: [field_1]}
  f2: {test: [field_2]}
  f3: {test: [field_3]}
  f4: {test: [field_4]}
  f5: {test: [field_5]}
  f6: {test: [field_6]}

# Vòng sàng chạy hai fold này trước (một ruộng bay cao không chồng tán, một
# ruộng bay trung chồng tán nhẹ) rồi mới chạy đủ 6.
screening: [f4, f2]

# Để đọc kết quả: 4 ruộng nội suy (điều kiện nằm trong khoảng train) và 2 ruộng
# ngoại suy (field_3 bay thấp nhất, field_5 chồng tán mạnh nhất) ghi riêng.
interpolation: [field_1, field_2, field_4, field_6]
extrapolation: [field_3, field_5]
