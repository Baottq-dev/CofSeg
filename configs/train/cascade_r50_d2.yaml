# Cascade Mask R-CNN R50-FPN — cùng backbone, FPN, đầu mask 28x28 và recipe
# với maskrcnn_r50_d2.yaml; chỉ khác ba tầng box head IoU 0.5/0.6/0.7.
# Chênh lệch với mốc 0 vì thế chỉ do phần định vị. Xem file base để biết cách chạy.
base: maskrcnn_r50_d2.yaml
name: cascade-r50-d2
model:
  arch: cascade
