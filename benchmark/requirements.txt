# Cả bốn model vào MỘT env.
#
#     python scripts/setup_env.py --only torch     # torch trước, tuỳ GPU
#     pip install -r benchmark/requirements.txt
#
# Muốn BỐN env riêng thì cài từng file, mỗi env một lệnh — xem
# benchmark/README.md mục "Dựng môi trường".
#
# Không file nào trong số này ghim torch: bản torch phụ thuộc GPU chứ không
# phụ thuộc model. Ghim trong từng file là cách chắc chắn để bốn file trôi ra
# xa nhau rồi không cài chung một env được nữa.
-r yolo11/requirements.txt
-r solov2/requirements.txt
-r maskrcnn/requirements.txt
-r mask2former/requirements.txt
