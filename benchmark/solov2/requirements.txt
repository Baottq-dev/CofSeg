# SOLOv2 (mmdet + mmcv). Người phụ trách: PhuongQuynh. CHỈ LINUX.
#
# CÀI torch TRƯỚC — file này cố ý KHÔNG ghim torch, xem benchmark/README.md.
#
#     pip install torch==2.11.0+cu130 torchvision==0.26.0+cu130 \
#       --index-url https://download.pytorch.org/whl/cu130
#
# Máy có nvcc 12.8/12.9 (phần lớn ảnh máy thuê) thì đổi cu130 -> cu128 ở cả
# hai chỗ: cùng phiên bản torch, chỉ khác bản dựng. Lệch major giữa nvcc và
# torch.version.cuda là detectron2/mmcv gãy lúc build_ext. Xem Bước 0.
#     pip install -r benchmark/solov2/requirements.txt
#     # rồi mmcv — xem benchmark/README.md mục "Cài mmcv"
#
# ĐỌC KỸ: model này kén phiên bản nhất trong bốn. mmcv chỉ có wheel dựng sẵn
# cho torch 2.4/cu121, mà tổ hợp đó ra đời TRƯỚC Blackwell nên SOLOv2 không
# chạy được trên RTX 50xx / RTX PRO 6000 / B200 nếu không build lại mmcv.
# benchmark/README.md mục "Build mmcv" nói đủ.

mmdet==3.3.0

# mmengine KHÔNG ghim ở đây: trên torch >= 2.6 bản PyPI hỏng. torch 2.6 đổi mặc
# định `torch.load(weights_only=True)`, còn checkpoint mmdet chứa dict `meta`
# với object tuỳ ý nên ném UnpicklingError lúc nạp trọng số COCO. mmengine
# 0.10.7 ra 04/03/2025, bản vá merge 25/10/2025 — không có bản phát hành nào
# sau bản vá. Lấy từ git; bản git chạy được với cả torch 2.4 lẫn 2.11:
#
#     pip install "git+https://github.com/open-mmlab/mmengine"

# ---- gói mà cofseg/ của thư mục này import
numpy==2.4.4
opencv-python==5.0.0.93
pycocotools==2.0.11
pyyaml==6.0.3
tqdm==4.68.3

# ---- mmcv KHÔNG nằm ở đây
#
# Cùng lý do detectron2 không nằm trong file của Mask R-CNN: cách cài phụ
# thuộc MÁY, không phụ thuộc phiên bản, mà một dòng requirements không rẽ
# nhánh theo GPU được.
#
#     torch 2.4/cu121  -> wheel dựng sẵn, vài giây
#     CUDA 12.8 / 13.x -> build từ nguồn, 20-120 phút
#
# Cả hai đường đều ở benchmark/README.md mục "Cài mmcv".
#
# ---- mmengine: với torch >= 2.6 phải lấy từ git, không dùng dòng ghim trên
#
# torch 2.6 đổi mặc định `torch.load(weights_only=True)`, còn checkpoint mmdet
# chứa dict `meta` với object tuỳ ý nên sẽ ném UnpicklingError lúc nạp trọng
# số COCO. mmengine 0.10.7 ra 04/03/2025, bản vá merge 25/10/2025 — bản trên
# PyPI ra trước bản vá bảy tháng:
#
#     pip install 'git+https://github.com/open-mmlab/mmengine'
