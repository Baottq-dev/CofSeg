# YOLO11-Seg (ultralytics). Người phụ trách: QuangBao.
#
# CÀI torch TRƯỚC — file này cố ý KHÔNG ghim torch, vì bản torch phụ thuộc
# GPU chứ không phụ thuộc model. Ghim ở đây thì bốn file model sẽ trôi ra xa
# nhau rồi không cài chung một env được nữa.
#
#     python scripts/setup_env.py --only torch              # GPU thường
#     python scripts/setup_env.py --only torch --cuda 130   # Blackwell (RTX 50xx)
#     pip install -r benchmark/yolo11/requirements.txt
#
# Xem benchmark/README.md mục "Dựng môi trường" cho bảng chọn phiên bản.
#
# KHÔNG cần `pip install -e .`: benchmark/yolo11/train.py tự thêm thư mục của
# nó vào sys.path nên nạp benchmark/yolo11/cofseg/, không phải canopyseg.

ultralytics==8.4.143

# ---- gói mà cofseg/ của thư mục này import
numpy==2.4.4
opencv-python==5.0.0.93
pycocotools==2.0.11
pyyaml==6.0.3
tqdm==4.68.3

# ---- CỐ Ý KHÔNG CÀI: albumentations
#
# Đây không phải thiếu sót. ultralytics 8.4.143 TỰ BẬT một loạt phép tăng
# cường khi thấy gói này nạp được, và im lặng bỏ qua khi không có:
#
#     class Albumentations:
#         try:
#             import albumentations as A
#             T = [A.Blur(p=0.01), A.ToGray(p=0.01), A.CLAHE(p=0.01), ...]
#         except ImportError:   # package not installed, skip
#
# Nghĩa là CÓ hay KHÔNG có gói này cho ra hai pipeline huấn luyện khác nhau,
# mà không dòng config nào của ta nói ra điều đó. Với bảng so sánh bốn model
# thì đó là biến ẩn. Để ngoài là lựa chọn có chủ đích; muốn bật thì thêm vào
# đây VÀ ghi vào phần phương pháp.
