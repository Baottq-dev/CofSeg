# Mask R-CNN (detectron2). Người phụ trách: VanNguyen. CHỈ LINUX.
#
# CÀI torch TRƯỚC — file này cố ý KHÔNG ghim torch, xem benchmark/README.md.
#
#     pip install torch==2.11.0+cu130 torchvision==0.26.0+cu130 \
#       --index-url https://download.pytorch.org/whl/cu130
#     pip install -r benchmark/maskrcnn/requirements.txt
#     # rồi detectron2 — xem benchmark/README.md

# ---- setuptools: KHÔNG được bỏ ghim
#
# detectron2 0.6 mở model zoo bằng `import pkg_resources`, mà setuptools 82 đã
# BỎ HẲN module đó (mở wheel ra đếm: 81.0.0 còn thư mục pkg_resources/, 82.0.1
# không còn file nào). Không ghim thì Mask R-CNN chết ở lần train đầu, SAU KHI
# đã nạp xong dữ liệu:
#
#     File ".../detectron2/model_zoo/model_zoo.py", line 4, in <module>
#         import pkg_resources
#     ModuleNotFoundError: No module named 'pkg_resources'
#
# Bẫy phụ: `import detectron2` KHÔNG kéo model_zoo vào, nên bước cài báo thành
# công rồi mới chết lúc train. Nên sau khi cài detectron2 phải kiểm cả
# `from detectron2 import model_zoo`, không chỉ `import detectron2`.
setuptools<82

# ---- gói mà cofseg/ của thư mục này import
numpy==2.4.4
opencv-python==5.0.0.93
pycocotools==2.0.11
pyyaml==6.0.3
tqdm==4.68.3

# ---- detectron2 KHÔNG nằm ở đây
#
# Nó build từ nguồn, và setup.py của nó quyết định có biên dịch op CUDA hay
# không TUỲ THEO lúc cài có thấy GPU và nvcc hay không — thứ mà một dòng
# requirements không điều khiển được. Để ở đây thì `pip install -r` trên máy
# có nvcc lệch phiên bản với torch sẽ chết giữa chừng.
#
# Tin tốt cho model này: `--skip-cuda-build` KHÔNG làm mất gì. detectron2
# không dùng op CUDA riêng nào cho Mask R-CNN — ROIAlign và NMS lấy của
# torchvision. (Mask2Former thì khác, xem file requirements của nó.)
